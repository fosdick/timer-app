// Yoga Timer App - Combined Theme Export

import { yogaColors } from './colors';
import { yogaTypography, fontWeights } from './typography';

// Re-export for direct imports
export { yogaColors, yogaTypography, fontWeights };

// Combined theme object for easy import
export const yogaTheme = {
  colors: yogaColors,
  typography: yogaTypography,
  fontWeights,
};

export default yogaTheme;
